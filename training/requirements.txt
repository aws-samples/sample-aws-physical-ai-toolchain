# Python dependencies for training scripts (local development)
# Install: pip install -r training/requirements.txt

# Data conversion (zarr → LeRobot v2)
zarr>=2.16
numpy>=1.24
opencv-python>=4.8
pandas>=2.0
pyarrow>=14.0

# Pipeline scripts
boto3>=1.34
pyyaml>=6.0

# Closed-loop evaluation (ZMQ transport)
pyzmq>=25

# Dataset download (optional — only if using HuggingFace datasets)
huggingface-hub>=0.20

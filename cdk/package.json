{
  "name": "aws-physical-ai-toolchain",
  "version": "0.1.0",
  "description": "AWS Physical AI Toolchain — Reference architecture for robotics RL training and edge deployment",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "destroy": "cdk destroy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.264.0",
    "constructs": "^10.3.0",
    "@aws-cdk/lambda-layer-kubectl-v31": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "~5.5.0",
    "aws-cdk": "^2.150.0"
  }
}
